
SavePath: "model/current_policy.model"
width: 11
height: 11
temp: 0.3  # 这个参数越小，mcts下棋时就越会选择搜索次数多的位置
n_playout: 300  # 如果为-1，那么就是动态搜索次数，则是固定搜索

mcts:
  dynamic_n_playout_max: 700  # 在mcts的搜索中，使用动态搜索次数时最大的搜索次数
  dynamic_n_playout_max_train: 1000  # 在mcts的搜索中，使用动态搜索次数时最大的搜索次数，（训练中）
